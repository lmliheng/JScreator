{
  "name": "backend",
  "version": "0.0.1",
  "private": true,
  "description": "JScreator Express 后端（TS 版）。构建与运行依赖统一由仓库根 package.json 提供；命令从根执行：npm run ts:build / npm run start:ts",
  "type": "module",
  "main": "dist/server.js",
  "engines": {
    "node": ">=20"
  }
}
