| 属性 | 类型 | 参考值 | 默认值 | 必填 |
|------|------|--------|--------|------|
| name | String | - | '' | 是 |
| color | String | - | '' | 否 |
| svgClass | String | - | '' | 否 |

## 插槽

| 名称 | 说明 |
|------|------|
| - | - |

## 事件

| 事件 | 描述 |
|------|------|
| - | - |

---

建议：
1. 组件已标记为弃用，建议移除或迁移到新方案
2. 如需继续使用，建议使用 `icon` 参数替代 `name`，保持命名一致性
3. 建议添加 `size` 属性，支持统一设置图标尺寸
4. 建议使用动态 import 按需加载图标
5. 建议考虑使用开源图标库组件（如 unplugin-icons）替代
6. 组件依赖全局注册的 SVG symbol，建议添加文档说明其依赖